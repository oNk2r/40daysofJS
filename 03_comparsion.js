console.log(2>1); //true or false;

// datatype should be same 

console.log(null >0 ); //f null is treated as 0;
console.log(null == 0); //f equality is diff
console.log(null >= 0); //t

// undefined --> false;
// === strict check

// data type => primitive and non primitive type [ reference type]

// memory ==> stack {primitive} , heap {non}

// object ,array refer to heap but intialized at stack